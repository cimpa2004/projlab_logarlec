package viewmodel;

public interface ICInit {
    /**
     * Ezt a függvényt a View-n hivja meg a Controller, amivel Viewban letrejon egy V objektum mely a paraméterként kapott
     * IV objektumot tárolja. Itt születik meg a kapcsolat a modellbeli és viewbeli objektumok között. IDvel lehet öket azonosítani
     * @param ivInstructor az IV objektom amit tarolni fog a letrehozott V objektum
     */
    public void CreateVInstructor(IVInstructor ivInstructor);
    /**
     * Ezt a függvényt a View-n hivja meg a Controller, amivel Viewban letrejon egy V objektum mely a paraméterként kapott
     * IV objektumot tárolja. Itt születik meg a kapcsolat a modellbeli és viewbeli objektumok között. IDvel lehet öket azonosítani
     * @param ivDoorSide az IV objektom amit tarolni fog a letrehozott V objektum
     */
    public void CreateVDoorSide(IVDoorSide ivDoorSide);
    /**
     * Ezt a függvényt a View-n hivja meg a Controller, amivel Viewban letrejon egy V objektum mely a paraméterként kapott
     * IV objektumot tárolja. Itt születik meg a kapcsolat a modellbeli és viewbeli objektumok között. IDvel lehet öket azonosítani
     * @param ivAirFreshener az IV objektom amit tarolni fog a letrehozott V objektum
     */
    public void CreateVAirFreshener(IVAirFreshener ivAirFreshener);
    /**
     * Ezt a függvényt a View-n hivja meg a Controller, amivel Viewban letrejon egy V objektum mely a paraméterként kapott
     * IV objektumot tárolja. Itt születik meg a kapcsolat a modellbeli és viewbeli objektumok között. IDvel lehet öket azonosítani
     * @param ivTVSZ az IV objektom amit tarolni fog a letrehozott V objektum
     */
    public void CreateVTVSZ(IVTVSZ ivTVSZ);
    /**
     * Ezt a függvényt a View-n hivja meg a Controller, amivel Viewban letrejon egy V objektum mely a paraméterként kapott
     * IV objektumot tárolja. Itt születik meg a kapcsolat a modellbeli és viewbeli objektumok között. IDvel lehet öket azonosítani
     * @param ivHolyBeerCup az IV objektom amit tarolni fog a letrehozott V objektum
     */
    public void CreateVHolyBeerCup(IVHolyBeerCup ivHolyBeerCup);
    /**
     * Ezt a függvényt a View-n hivja meg a Controller, amivel Viewban letrejon egy V objektum mely a paraméterként kapott
     * IV objektumot tárolja. Itt születik meg a kapcsolat a modellbeli és viewbeli objektumok között. IDvel lehet öket azonosítani
     * @param ivWetTableCloth az IV objektom amit tarolni fog a letrehozott V objektum
     */
    public void CreateVWetTableCloth(IVWetTableCloth ivWetTableCloth);
    /**
     * Ezt a függvényt a View-n hivja meg a Controller, amivel Viewban letrejon egy V objektum mely a paraméterként kapott
     * IV objektumot tárolja. Itt születik meg a kapcsolat a modellbeli és viewbeli objektumok között. IDvel lehet öket azonosítani
     * @param ivCamembert az IV objektom amit tarolni fog a letrehozott V objektum
     */
    public void CreateVCamembert(IVCamembert ivCamembert);
    /**
     * Ezt a függvényt a View-n hivja meg a Controller, amivel Viewban letrejon egy V objektum mely a paraméterként kapott
     * IV objektumot tárolja. Itt születik meg a kapcsolat a modellbeli és viewbeli objektumok között. IDvel lehet öket azonosítani
     * @param ivFFP2Mask az IV objektom amit tarolni fog a letrehozott V objektum
     */
    public void CreateVFFP2Mask(IVFFP2Mask ivFFP2Mask);
    /**
     * Ezt a függvényt a View-n hivja meg a Controller, amivel Viewban letrejon egy V objektum mely a paraméterként kapott
     * IV objektumot tárolja. Itt születik meg a kapcsolat a modellbeli és viewbeli objektumok között. IDvel lehet öket azonosítani
     * @param ivSlideRule az IV objektom amit tarolni fog a letrehozott V objektum
     */
    public void CreateVSlideRule(IVSlideRule ivSlideRule);
    /**
     * Ezt a függvényt a View-n hivja meg a Controller, amivel Viewban letrejon egy V objektum mely a paraméterként kapott
     * IV objektumot tárolja. Itt születik meg a kapcsolat a modellbeli és viewbeli objektumok között. IDvel lehet öket azonosítani
     * @param ivRoom az IV objektom amit tarolni fog a letrehozott V objektum
     */
    public void CreateVRoom(IVRoom ivRoom);
    /**
     * Ezt a függvényt a View-n hivja meg a Controller, amivel Viewban letrejon egy V objektum mely a paraméterként kapott
     * IV objektumot tárolja. Itt születik meg a kapcsolat a modellbeli és viewbeli objektumok között. IDvel lehet öket azonosítani
     * @param ivTransistor az IV objektom amit tarolni fog a letrehozott V objektum
     */
    public void CreateVTransistor(IVTransistor ivTransistor);
    /**
     * Ezt a függvényt a View-n hivja meg a Controller, amivel Viewban letrejon egy V objektum mely a paraméterként kapott
     * IV objektumot tárolja. Itt születik meg a kapcsolat a modellbeli és viewbeli objektumok között. IDvel lehet öket azonosítani
     * @param ivItem az IV objektom amit tarolni fog a letrehozott V objektum
     */
    public void CreateVItem(IVItem ivItem);
    /**
     * Ezt a függvényt a View-n hivja meg a Controller, amivel Viewban letrejon egy V objektum mely a paraméterként kapott
     * IV objektumot tárolja. Itt születik meg a kapcsolat a modellbeli és viewbeli objektumok között. IDvel lehet öket azonosítani
     * @param ivJanitor az IV objektom amit tarolni fog a letrehozott V objektum
     */
    public void CreateVJanitor(IVJanitor ivJanitor);
    /**
     * Ezt a függvényt a View-n hivja meg a Controller, amivel Viewban letrejon egy V objektum mely a paraméterként kapott
     * IV objektumot tárolja. Itt születik meg a kapcsolat a modellbeli és viewbeli objektumok között. IDvel lehet öket azonosítani
     * @param ivStudent az IV objektom amit tarolni fog a letrehozott V objektum
     */
    public void CreateVStudent(IVStudent ivStudent);

}
